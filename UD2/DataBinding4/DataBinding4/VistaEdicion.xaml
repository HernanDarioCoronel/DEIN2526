<Window x:Class="DataBinding4.VistaEdicion"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataBinding4"
        mc:Ignorable="d"
        Title="VistaEdicion" Height="300" Width="300">
    <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="120"/>
        </Grid.ColumnDefinitions>

        <Label Content="Nombre:" HorizontalAlignment="Right"/>
        <TextBox Grid.Column="1" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>

        <Label Grid.Row="1" Content="Edad:" VerticalAlignment="Center" HorizontalAlignment="Right"/>
        <Slider Grid.Row="1" Grid.Column="1" Margin="10" Minimum="0" Maximum="100" Value="{Binding Edad, Mode=TwoWay}"/>
        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding Edad}" HorizontalAlignment="Center"/>

        <CheckBox HorizontalAlignment="Center" Grid.ColumnSpan="2" Grid.Row="3" Content="Es Estudiante" IsChecked="{Binding Estudiante, Mode=TwoWay}" Margin="0,10"/>

    </Grid>
</Window>
