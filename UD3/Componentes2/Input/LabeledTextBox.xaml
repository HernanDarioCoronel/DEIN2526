<UserControl x:Class="Input.LabeledTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Input"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid d:Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="90*"/>
        </Grid.ColumnDefinitions>
        <d:Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </d:Grid.RowDefinitions>
        <Label 
            HorizontalAlignment="Left"
            VerticalAlignment="Center" 
            Margin="5"
            Content="{Binding LabelText, UpdateSourceTrigger=PropertyChanged}"
            />

        <TextBox 
            Width="{Binding TextBoxWidth, UpdateSourceTrigger=PropertyChanged}"
            Grid.Column="1" 
            HorizontalAlignment="Left"
            Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}"
            />
    </Grid>
</UserControl>
