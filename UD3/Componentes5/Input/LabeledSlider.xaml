<UserControl 
    x:Name="Root"
    x:Class="Input.LabeledSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Input"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">
    <Grid VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30*"/>
            <ColumnDefinition Width="60*"/>
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>
        <Label 
            FontSize="24" 
            Grid.Column="0" 
            Content="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
            />
        <Slider 
            VerticalAlignment="Center" 
            Grid.Column="1" 
            Minimum="{Binding MinValue, RelativeSource={RelativeSource AncestorType=UserControl}}" 
            Maximum="{Binding MaxValue, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Value="{Binding Value, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
            />
        <TextBox 
            FontSize="24" 
            Grid.Column="2" 
            Text="{Binding Value, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Background="{Binding Color, RelativeSource={RelativeSource AncestorType=UserControl}}"
            />
    </Grid>
</UserControl>
