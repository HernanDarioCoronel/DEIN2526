<Window xmlns:Input="clr-namespace:Input;assembly=Input"  x:Class="Componentes3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Componentes3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Input:LabeledSlider 
            Name="sliderMinimo"
            Grid.Row="0" 
            Label="Minimo"
            MinValue="-100"
            MaxValue="100" 
            Value="0"
            />
        <Input:LabeledSlider 
            Name="sliderMaximo"
            Grid.Row="1" 
            Label="Maximo" 
            MinValue="-100"
            MaxValue="100" 
            Value="0"
            />
        <Input:LabeledSlider 
            Name="sliderDecimales"
            Grid.Row="2" 
            Label="Decimales" 
            MinValue="0" 
            MaxValue="10" 
            Value="1"
            />
        <Input:NumericInput 
            Grid.Row="3" 
            Width="200" 
            JumpValue="1"
            MinValue="{Binding ElementName=sliderMinimo, Path=Value}"
            MaxValue="{Binding ElementName=sliderMaximo, Path=Value}"
            Decimals="{Binding ElementName=sliderDecimales, Path=Value}"
            />
    </Grid>
</Window>
